{
  "name": "root",
  "version": "3.15.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/deprecated/*",
    "packages/embeds/*",
    "packages/forge/*",
    "packages/forge/blocks/*",
    "packages/blocks/*",
    "apps/*"
  ],
  "scripts": {
    "postinstall": "turbo run db:generate",
    "prepare": "husky",
    "pre-commit": "turbo run format-and-lint lint-repo check-broken-links test",
    "format-and-lint": "biome check .",
    "format-and-lint:fix": "biome check . --write --unsafe",
    "lint-repo": "sherif -r unordered-dependencies -r packages-without-package-json --ignore-package @typebot.io/legacy --ignore-package bot-engine",
    "lint-repo:fix": "npm run lint-repo -- --fix",
    "patch-embed-libs": "tsx packages/scripts/src/patchEmbedLibs.ts && npm install",
    "start-docker-compose-dev": "docker compose -f docker-compose.dev.yml up",
    "build-docker-compose-prod": "docker compose -f docker-compose.build.yml build",
    "start-docker-compose-prod": "docker compose -f docker-compose.yml up",
    "build": "turbo run typecheck build --filter=builder... --filter=viewer... --filter=landing-page... --filter=docs...",
    "dev": "turbo dev --filter=builder... --filter=viewer... --filter=landing-page --filter=!@typebot.io/inngest --filter=!@typebot.io/prisma --filter=!@typebot.io/partykit --concurrency=11",
    "sync-locales": "dotenv -e .env -- tolgee sync --continue-on-warning --remove-unused --no-strict-namespace --patterns './apps/builder/src/**/*.ts?(x)' --backup './.tolgee/backup'",
    "pull-locales": "dotenv -e .env -- tolgee pull && npm run format-and-lint:fix",
    "push-locales": "dotenv -e .env -- tolgee push",
    "create-new-block": "cd packages/forge/cli && npm start",
    "check-unused-dependencies": "tsx packages/scripts/src/checkDependencies.ts",
    "restore": "echo 'Cleaning...' && npx rimraf --glob '**/{node_modules,dist,.next,.turbo}'"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.7",
    "@tolgee/cli": "^2.14.0",
    "husky": "^9.1.7",
    "sherif": "^1.9.0",
    "turbo": "^2.6.1",
    "typescript": "^5.9.3",
    "rimraf": "^6.1.2",
    "tsx": "^4.19.2",
    "zx": "^8.2.3",
    "vitest": "^2.1.8",
    "jsdom": "^25.0.1",
    "@vitest/coverage-v8": "^2.1.8"
  },
  "packageManager": "pnpm@9.15.2",
  "engines": {
    "node": ">=20"
  },
  "trustedDependencies": [
    "@biomejs/biome",
    "@prisma/client",
    "@prisma/engines",
    "@sentry/cli",
    "@swc/core",
    "core-js",
    "esbuild",
    "isolated-vm",
    "prisma",
    "sharp",
    "inngest-cli"
  ]
}
